apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: timestamp-workflow
  namespace: argo
spec:
  entrypoint: whalesay
  podRetention: OnFailure
  templates:
  - name: whalesay
    container:
      image: docker/whalesay:latest
      command: [sh, -c]
      args:
        - |
          echo "Timestamp: $(date)" | cowsay
          sleep 60
